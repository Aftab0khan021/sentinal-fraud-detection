# SentinAL Environment Variables

# JWT Configuration
JWT_SECRET_KEY=CHANGE_THIS_IN_PRODUCTION_use_secrets_token_urlsafe_32
JWT_REFRESH_SECRET_KEY=CHANGE_THIS_REFRESH_KEY_IN_PRODUCTION_use_secrets_token_urlsafe_32
JWT_EXPIRATION_MINUTES=30
JWT_REFRESH_EXPIRATION_DAYS=7

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=sentinal_redis_2024_CHANGE_IN_PRODUCTION
REDIS_DB=0
REDIS_ENABLED=true

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
RATE_LIMIT_PER_MINUTE=10
ENVIRONMENT=development

# Ollama Configuration
OLLAMA_MODEL=llama3.2:1b

# Grafana Configuration (for Docker Compose)
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=SentinAL_Grafana_2024_CHANGE_IN_PRODUCTION

# Instructions:
# 1. Copy this file to .env in the python/ directory
# 2. Generate secure secrets:
#    python -c "import secrets; print(secrets.token_urlsafe(32))"
# 3. Replace all CHANGE_THIS values with generated secrets
# 4. Never commit .env file to version control
